# ===================================================================
# Spring Boot configuration for the "heroku" profile.
#
# This configuration overrides the application.yml file.
# ===================================================================

# ===================================================================
# Standard Spring Boot properties.
# Full reference is available at:
# http://docs.spring.io/spring-boot/docs/current/reference/html/common-application-properties.html
# ===================================================================

eureka:
    instance:
        hostname: bb-skygreen-2001.herokuapp.com
        non-secure-port: 80
        prefer-ip-address: false
    client:
        service-url:
            defaultZone: ${JHIPSTER_REGISTRY_URL}/eureka/

spring:
    datasource:
        type: com.zaxxer.hikari.HikariDataSource
        url: ${JDBC_DATABASE_URL}
        username: ${JDBC_DATABASE_USERNAME}
        password: ${JDBC_DATABASE_PASSWORD}

##       MySQL start
#        url: jdbc:mysql://localhost:3306/bb?useUnicode=true&characterEncoding=utf8&useSSL=false
#        username: root
#        password:

#        url: jdbc:mysql://debug.betterlife.com:3306/bb?useUnicode=true&characterEncoding=utf8&useSSL=false
#        username: root
#        password:
##       MySQL end

##       PostgreSQL start
#        url: jdbc:postgresql://localhost:5432/bb
#        username: bb
#        password: bb2017
##       PostgreSQL end
        hikari:
            maximumPoolSize: 8
